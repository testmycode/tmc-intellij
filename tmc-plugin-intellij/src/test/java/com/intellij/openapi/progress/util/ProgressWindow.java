package com.intellij.openapi.progress.util;

/*
This is a simple workaround for tests. For whatever reason Mockito
fails at creating a mock of ThreadingService.
 */
public interface ProgressWindow {
}
